# TODO — 70k/300 low-concurrency sweep (DeepSeek-V4-Pro, 8×MI355X)

Workload: **ISL 70000 / OSL 300, concurrency {2,4,8,16,32}**, SGLang-only.
Full results + method: `EXPERIMENT_LOG_2026-06-25.md`. Summary in `HANDOFF.md`.
Launch: `run_sgl_dsv4_70k.sh` (MODE/CHUNK/SWA/MEM/DELAYER). Client:
`sweep_dsv4_sglang_client.sh` (NP_MULT=4 WARM_MULT=1).

## Best config found (one-liner)
- conc 2/4 → `MODE=tp8 CHUNK=32768`
- conc 8/16/32 → `MODE=tp8dp8 CHUNK=16384 SWA=0.1 MEM=0.80 DELAYER=off`
- `MODE=tp8dp8 CHUNK=16384 SWA=0.1 MEM=0.80 DELAYER=off PORT=8000 bash run_sgl_dsv4_70k.sh`

## Best-of results table (InferenceX-style; E2EL/TTFT/ITL in ms; interactivity=1000/median ITL)
| Input_len | output_len | TP,DP,EP | Concurrency | TTT (tok/s) | Median E2EL (ms) | Median TTFT (ms) | Median ITL (ms) | Interactivity (tok/s/user) |
|---:|---:|:--|---:|---:|---:|---:|---:|---:|
| 70000 | 300 | TP8 (DP1,EP1)  | 2  | 13,937.0 | 10,083.0 | 4,941.6  | 14.369 | 69.59 |
| 70000 | 300 | TP8 (DP1,EP1)  | 4  | 17,655.1 | 15,912.0 | 8,125.6  | 14.821 | 67.47 |
| 70000 | 300 | TP8,DP8 (EP1)  | 8  | 29,193.6 | 19,235.5 | 12,959.2 | 20.821 | 48.03 |
| 70000 | 300 | TP8,DP8 (EP1)  | 16 | 36,779.4 | 30,533.7 | 17,395.0 | 23.089 | 43.31 |
| 70000 | 300 | TP8,DP8 (EP1)  | 32 | 42,139.8 | 53,340.2 | 27,285.3 | 28.066 | 35.63 |

### Commands that produced the best data (cwd: useful-scripts/benchmarking/dsv4/)
conc 2,4 (TP8):
```
MODE=tp8 CHUNK=32768 SWA=0.1 MEM=0.92 PORT=8000 bash run_sgl_dsv4_70k.sh
PYTHONPATH=/sgl-workspace/sglang-upstream/python:/sgl-workspace/mori:/sgl-workspace/aiter \
RESULT_DIR=/workspace/bench_results_dsv4_70k/tp8_chunk32768 \
BENCH="python3 -m sglang.bench_serving" BACKEND=sglang-oai \
WORKLOADS="70000:300" CONCS="2 4" NP_MULT=4 WARM_MULT=1 bash sweep_dsv4_sglang_client.sh
```
conc 8,16,32 (TP8+DP-attn, delayer off, mem 0.80):
```
MODE=tp8dp8 CHUNK=16384 SWA=0.1 MEM=0.80 DELAYER=off PORT=8000 bash run_sgl_dsv4_70k.sh
PYTHONPATH=/sgl-workspace/sglang-upstream/python:/sgl-workspace/mori:/sgl-workspace/aiter \
RESULT_DIR=/workspace/bench_results_dsv4_70k/tp8dp8_chunk16384_nodelayer \
BENCH="python3 -m sglang.bench_serving" BACKEND=sglang-oai \
WORKLOADS="70000:300" CONCS="8 16 32" NP_MULT=4 WARM_MULT=1 bash sweep_dsv4_sglang_client.sh
```

## Done
- [x] Pin sglang-upstream via PYTHONPATH (namespace-shadow fix)
- [x] Phase 1 — TP8 sweep conc 2–32 (saturates ~conc8)
- [x] Phase 2 — DP-attention sweep conc 8–32 (scales; needs mem 0.80 or MoE OOM)
- [x] Phase 3 — chunk sweep (16384 vs 32768 = neutral; pick 16384/rank)
- [x] Phase 3.5 — swa-ratio / DSV4 pool analysis (TP8 c32 mem-edge)
- [x] Phase 4 — prefill-delayer ON vs OFF (OFF wins +6–16%)
- [x] Write EXPERIMENT_LOG_2026-06-25.md + update HANDOFF.md + index

## Next / open
- [ ] ATOM comparison at 70k/300 (apples-to-apple, same client) — engine A/B
- [ ] conc > 32 and/or longer context (128k?) — DP per-rank memory will start to
      bite; re-tune swa/mem then
- [ ] EP / mori-EP path at 70k/300 (not tried)
- [ ] DP-attention at conc 2/4 — confirm (with data) it loses to TP8 due to idle
      ranks (currently assumed/ skipped by design)
- [ ] TP8 chunk 65536 at conc 2/4 — does a bigger chunk help the low-conc tail?
- [ ] Decide if any of these findings warrant a doc/PR (e.g. delayer-off default
      for long-context low-conc, or DSV4 swa-pool sizing guidance)

## Gotchas to remember
- DP MoE runs on the GATHERED global batch (per-rank chunk × dp) → mem-fraction
  must be ≤ 0.80 or `fused_moe` stage2 OOMs.
- DSV4 uses `DSV4PoolConfigurator`: swa sub-pool = full × swa_full_tokens_ratio;
  it binds TP8 at conc=32 (swa usage→1.0). conc32×70k is at the single-node mem
  ceiling in TP8.
- prefill-delayer is regime-dependent: OFF for long-context prefill-dominated
  low-conc; ON for 8k/1k high-conc decode-heavy.
- Server kill recipe (frees all VRAM): kill rocm-smi --showpids + pkill -9 -f
  sglang; wait ~15–20s; confirm with rocm-smi.
