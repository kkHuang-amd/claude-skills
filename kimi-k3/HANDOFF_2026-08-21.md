# Kimi-K3 handoff — 2026-08-21

## CONTINUE HERE

**Status:** Two independent default-off decode candidates are implemented in
SGLang. KDA input-projection MXFP4 passes at exact M32/M64 (`1.020x`/`1.428x`).
The newly corrected MLA shared-input PTPC path quantizes normalized hidden once
for both QKV-A and g_proj, retains SGLang's fused output gate, and passes at
exact M64 (`34.320 -> 29.920 us`, `1.1471x`); M32 loses and is excluded.
Production remains unchanged. Details:
`aiter-optimization-tracker/KDA_INPROJ_MXFP4_M32_M64_2026-08-23.md` and
`aiter-optimization-tracker/MLA_SHARED_PTPC_M64_2026-08-23.md`.

**Decision:** Production is unchanged. C2 has a `+7.989 ms` ATOM graph gap.
At C64 ATOM's graph is `-2.377 ms` shorter, but its endpoint TPOT is
`+12.956 ms`; the approximately `12.21 ms` cadence/request discrepancy remains
unresolved and graph-external analysis is deferred. Armed routes show mean
active experts `133.87` SGLang versus `18.03` ATOM; matched replay makes A8W4
faster by `3.43%`/`7.44%`, so the trace inversion is route workload rather than
A16W4 superiority. The combined dense prepared policy costs
`3,273,054,208 B` (`3.048269 GiB/GPU`) and must not be promoted as one bundle.
The rejected QKV-A candidate's apparent total graph improvement
(`31.012 -> 30.566 ms`) is routed-MoE/collective/route-work variation, not a
QKV-A win. Graph-external analysis remains deferred.

**Next:** Measure each candidate's real TP8 capacity separately, then run
matched common-client C2/C64 and complete prefill/decode retraces without
bundling candidates. For MLA shared PTPC, require the combined QKV-A + gate
boundary and total C64 replay to move in the predicted direction before
endpoint correctness gates.

Preserve:

```text
/workspace/kimi-k3-runs/common-oai-sglang-atom-traces-2026-08-22/route-validation/
/workspace/kimi-k3-runs/matched-route-current-kernel-2026-08-23/
```

Do not commit, push, discard, reset, or rewrite either worktree without an
explicit request.

## Worktrees

### SGLang

```text
path:   /sgl-workspace/sglang-k3-triton37
branch: perf/k3_opts_0812
HEAD:   455b744aa77b2078de7577619dc12d2775fc1091
remote: origin/perf/k3_opts_0812
```

Uncommitted files:

```text
M  docs/docs/references/environment_variables.mdx
M  python/sglang/kernels/ops/kimi_k3/latent_mxfp4_aiter_hip.py
M  python/sglang/srt/environ.py
M  python/sglang/srt/models/kimi_k3.py
M  test/registered/kernels/ops/kimi_k3/test_latent_mxfp4_aiter_hip.py
?? python/sglang/kernels/ops/kimi_k3/kda_inproj_mxfp4_aiter_hip.py
?? python/sglang/kernels/ops/kimi_k3/mla_shared_ptpc_aiter_hip.py
?? test/registered/kernels/ops/kimi_k3/test_kda_inproj_mxfp4_aiter_hip.py
?? test/registered/kernels/ops/kimi_k3/test_mla_shared_ptpc_aiter_hip.py
```

These include the pre-existing latent MXFP4 changes, the interrupted KDA
input-projection MXFP4 candidate, and the separate completed MLA shared-PTPC
candidate. Do not conflate or remove one while changing another. The PR #35770
attention files and its additions to `environ.py`/environment documentation
were removed using the preserved pre-port patch snapshot. The rejected
streamed-B FlyDSL dense path was also removed from the retained SGLang patch.

### AITER

```text
path:   /sgl-workspace/aiter-k3-triton37
branch: integration/k3-core-only
HEAD:   b56d27beee1ce0132ca868da24d1a25e88e6ef42
remote: origin/integration/k3-core-only
```

The worktree is clean. The uncommitted MXFP4 GEMM2/tail Route A and dense
epilogue experiments were rejected for production and rolled back at the
user's request.

### Current AITER integration candidate

```text
path:   /sgl-workspace/aiter-atom-current
branch: main
HEAD:   dc4bdf1c142181ad90b7f6948564126df4c05fde
remote: origin/main
```

Uncommitted migration:

```text
M  aiter/configs/model_configs/kimik3_bf16_tuned_gemm.csv
M  aiter/fused_moe.py
M  aiter/ops/flydsl/moe_kernels.py
?? op_tests/test_fused_moe_output_buffer.py
?? op_tests/test_flydsl_stage1_out_cache.py
```

These restore the K3 integration branch's caller-owned MoE output, default-off
stage1 scratch reuse, and 27 N4480/N6016 BF16 tuning rows on current main.
Preserve them until the user decides whether to keep the candidate.

## PR #35770 Triton prefill port

Worktree disposition: removed on 2026-08-21. The following records describe
the completed evaluation and are retained as historical evidence.

Source feature commit:

```text
be31ec565da7b10b248148d94ad546e819084ef7
```

Integrated:

- gfx950 split-dimension absorbed MLA for exact `12Q/1KV, Lq576/Lv512`
- fail-closed dispatch and generic fallback
- two-stage scheduling, exp2 accumulation and natural-log LSE conversion
- native FP8 zero-prefix casts and scaled cached-prefix handling
- one public policy:
  `SGLANG_K3_TRITON_PREFILL_PROFILE=off|bf16|fp8`
- default remains `off`
- compatibility flag `SGLANG_TRITON_FP8_PREFILL_ATTN`, default false

Focused test:

```text
test/registered/unit/layers/attention/test_triton_mla_prefill_gfx950.py
4 passed
```

Micro highlights:

```text
fresh BF16 8K:          1.611x, rel-L2 0.000030
fresh FP8 8K:           1.821x, rel-L2 0.036327
fresh BF16 16K:         1.684x, rel-L2 0.000031
fresh FP8 16K:          1.919x, rel-L2 0.036843
absorbed BF16 8K+8K:    2.142x, rel-L2 0.000044
absorbed FP8 8K+8K:     1.076x, rel-L2 0.000034
```

TP8 final backend A/B, Triton FP8 versus production AITER prefill:

```text
8K/1K throughput geomean ratio:   0.9884
68K/350 throughput geomean ratio: 0.7292
all requests completed; no OOM/retractions
```

Accuracy/correctness:

```text
GSM8K-50:   AITER 1.000, Triton FP8 1.000
GSM8K-1319: AITER 0.958, Triton FP8 0.947
68K paired first-token top-1 match: 81.25%
top-20 probability cosine: mean 0.97965, min 0.95694
```

Verdict: isolated kernels pass; production promotion fails.

Primary report:

```text
/workspace/claude-skills/kimi-k3/aiter-optimization-tracker/TRITON_PREFILL_PR35770_2026-08-21.md
```

Raw artifacts and patch snapshots:

```text
/workspace/kimi-k3-runs/triton-prefill-pr35770-2026-08-21/
```

The artifact directory contains `preexisting-sglang.patch`,
`final-sglang.patch`, `SHA256SUMS`, micro outputs, endpoint summaries, server
effective arguments, GSM8K logs, and paired long-context responses.

## Other completed MXFP4 work

Current production recommendation remains the capacity-aware K3 profile:

- latent-up-only MXFP4 for large token batches
- gfx950 ASM retained for the rejected dense/tail FlyDSL alternatives
- default-off RMSNorm+MXFP4 fusion because endpoint benefit was neutral
- default-off M32-M256 decode latent-up MXFP4 band; modest C32/C64 gain
- production AITER prefill and Triton decode

Read these only when working in the corresponding area:

```text
/workspace/claude-skills/kimi-k3/aiter-optimization-tracker/MOE_LATENT_SPLIT_2026-08-20.md
/workspace/claude-skills/kimi-k3/aiter-optimization-tracker/MXFP4_DENSE_KERNEL_TUNING_2026-08-20.md
/workspace/claude-skills/kimi-k3/aiter-optimization-tracker/MXFP4_TAIL_EPILOGUE_REJECTED_2026-08-20.md
/workspace/claude-skills/kimi-k3/aiter-optimization-tracker/RMSNORM_MXFP4_FUSION_2026-08-20.md
/workspace/claude-skills/kimi-k3/aiter-optimization-tracker/DECODE_LATENT_MXFP4_2026-08-20.md
```

## Current ATOM C2 multi-stream reference

Current ATOM `27f8639b` plus isolated AITER main `dc4bdf1c` was run with the
Kimi-K3 recipe on this machine. Five fixed 8192/1024 C2 rounds per side gave:

```text
five-run median throughput: multi 780.43 vs single 767.18 tok/s (+1.73%)
five-run median TPOT:       multi  22.17 vs single  22.64 ms    (-2.08%)
all measured requests:      160/160 successful
```

The multi-stream side warmed upward, so use the median rather than the
`+3.99%` mean gain as the conservative result. Full method, revisions,
per-round values, ATOM fork/join behavior, and SGLang implementation gates:

```text
/workspace/claude-skills/kimi-k3/aiter-optimization-tracker/ATOM_C2_STREAM_AB_2026-08-21.md
```

Artifacts:

```text
/workspace/kimi-k3-runs/atom-c2-stream-ab-2026-08-21/
```

The matched C64 follow-up decisively rejects the same policy at 64 tokens:

```text
throughput: multi 8680.09 vs single 9878.91 tok/s (-12.14%)
TTFT:       multi 15695.19 vs single 14299.91 ms    (+9.76%)
TPOT:       multi    51.22 vs single    44.65 ms    (+14.70%)
requests:   512/512 successful per side
```

Graph inspection proves the switch was active. Full report:

```text
/workspace/claude-skills/kimi-k3/aiter-optimization-tracker/ATOM_C64_STREAM_AB_2026-08-21.md
```

Artifacts:

```text
/workspace/kimi-k3-runs/atom-c64-stream-ab-2026-08-21/
```

## Common-client SGLang versus ATOM C64

Using the same current AITER `dc4bdf1c`, global FlyDSL 0.3.1, one streaming
OpenAI client, and one exact prompt manifest removes the apparent 10% gap:

```text
throughput: SGLang 9800.69 vs ATOM 9846.50 tok/s (ATOM +0.47%)
TTFT:       SGLang 27579.66 vs ATOM 14689.19 ms    (ATOM -46.74%)
TPOT:       SGLang    31.74 vs ATOM    44.70 ms    (ATOM +40.82%)
E2E:        SGLang 60029.54 vs ATOM 59643.74 ms    (ATOM -0.64%)
requests:   512/512 successful per engine
```

The engines have different scheduling shapes, not a meaningful throughput
separation: SGLang delays most first tokens then decodes faster; ATOM staggers
first tokens and decodes slower. Full report:

```text
/workspace/claude-skills/kimi-k3/aiter-optimization-tracker/COMMON_CLIENT_SGLANG_ATOM_C64_2026-08-21.md
```

Artifacts and shared manifest:

```text
/workspace/kimi-k3-runs/common-oai-sglang-atom-c64-2026-08-21/
```

The same controlled C2 comparison shows a real low-concurrency gap:

```text
throughput: SGLang 1138.02 vs ATOM 768.04 tok/s (ATOM -32.51%)
TTFT:       SGLang  960.14 vs ATOM 870.18 ms    (ATOM  -9.37%)
TPOT:       SGLang   14.90 vs ATOM  22.62 ms    (ATOM +51.77%)
E2E:        SGLang 16192.43 vs ATOM 23997.45 ms (ATOM +48.20%)
requests:   80/80 successful per engine
```

Full report and artifacts:

```text
/workspace/claude-skills/kimi-k3/aiter-optimization-tracker/COMMON_CLIENT_SGLANG_ATOM_C2_2026-08-21.md
/workspace/kimi-k3-runs/common-oai-sglang-atom-c2-2026-08-21/
```

Use matched compact C2 traces to attribute this decode-path gap before deciding
which ATOM/SGLang implementation should be ported.

With explicit user approval, global FlyDSL was upgraded from 0.2.4 to 0.3.1
for current AITER main compatibility. ATOM and the new AITER checkout remain
source-only and were not installed into the Python environment.

## SGLang current-AITER C2 result

Current AITER main was adapted with the missing K3 MoE output/scratch/config
work and validated against the existing integration:

```text
five-run median throughput: old 1146.00 vs current 1136.17 tok/s (-0.86%)
five-run median TTFT:        old  844.71 vs current  871.71 ms    (+3.20%)
five-run median TPOT:        old   14.86 vs current   14.99 ms    (+0.87%)
requests:                    80/80 successful per side
vendored K3 FlyDSL suite:    50 passed
```

Do not promote current AITER yet. Full migration details, the discarded
duplicate-header diagnostic run, per-round values, and next attribution:

```text
/workspace/claude-skills/kimi-k3/aiter-optimization-tracker/CURRENT_AITER_SGLANG_C2_AB_2026-08-21.md
```

Artifacts:

```text
/workspace/kimi-k3-runs/sglang-current-aiter-ab-2026-08-21/
```

## AITER dependency answers already established

SGLang PR #34198:

- original hard dependency: AITER #4495, commit `89fce019`
- provided gfx950 FlyDSL KDA decode plus fused `128x128 f_b`
- later vendored into SGLang, removing the hard dependency on the unreleased
  `aiter.ops.flydsl.kimi_k3_kda_decode` module

SGLang PR #35287:

- no hard dependency on an unmerged AITER PR
- validated against pinned AITER `d9e5ef7`
- vendored feature provenance:
  AITER #4497 MLA gate, #4499 KDA group64, #4504 FP8 preroute,
  #4503 optional latent tail
- explicitly does not require #4617 or #4647
- excludes MLA Q/cache behavior requiring #4342 / `770790cd`

## Cleanup completed

All rebuildable K3-specific directories matching the known
`/tmp/aiter-jit-*` and `/tmp/sglang-cache-*` experiment paths were removed.
Durable `/workspace/kimi-k3-runs` benchmark evidence was retained.

## Reusable script toolkit

Reusable analysis, trace, endpoint, accuracy, contract-dump, and microbenchmark
helpers are organized under:

```text
/workspace/useful-scripts/benchmarking/kimi-k3/
```

Start with its `README.md`. New categorized tools include:

```text
analysis/analyze_chrome_trace.py
analysis/analyze_rocprof_db.py
analysis/summarize_endpoint_logs.py
analysis/compare_generate_outputs.py
contracts/tensor_contract_dump.py
contracts/compare_tensor_contracts.py
endpoint/run_serving_matrix.sh
accuracy/run_long_context_logprobs.py
trace/capture_compact_profile.sh
server/dump_runtime_state.sh
micro/microbench_common.py
```

The root deployment/B300 scripts remain in place for command-line
compatibility. New experiment wrappers should call the categorized tools rather
than copy endpoint loops, trace capture, kernel taxonomy, or graph timing code.

Persistent Cursor-rule sources for future agents:

```text
/workspace/claude-skills/kimi-k3/cursor-rules/
```

After replacing an image/workspace, install the durable rules with:

```bash
bash /workspace/claude-skills/kimi-k3/install_cursor_rules.sh /sgl-workspace
```

## Fresh-chat prompt

```text
Run `bash /workspace/claude-skills/kimi-k3/install_cursor_rules.sh
/sgl-workspace`, then read
`/workspace/claude-skills/kimi-k3/HANDOFF_2026-08-21.md` and continue from its
CONTINUE HERE section. Apply the reduce-conversation-usage skill. Treat the
handoff and linked durable reports as the source of truth; do not reconstruct
the previous chat. Preserve all existing uncommitted SGLang and AITER changes,
and do not commit/push/reset unless I explicitly request it.
```
