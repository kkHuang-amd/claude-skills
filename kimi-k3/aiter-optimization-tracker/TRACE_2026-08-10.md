# Kimi-K3 decode trace evidence — 2026-08-10

Runtime:

```text
SGLang: 8950e2e0d + 0dae20b83
AITER: integration/kimi-k3-pr-stack with staged #4495/#4617
Torch: 2.9.1+rocm7.2.0
Triton: 3.6.0+git42270451
Hardware: 8× MI355X / gfx950
```

Artifacts:

```text
/workspace/claude-skills/kimi-k3/
  stage2-runs/2026-08-10-optimization-execution/phase1-trace/
```

## Method

- Warm TP8 server with full K3 AITER flags and
  `SGLANG_K3_KDA_FUSED_BACKEND=aiter`.
- Profile a 512/128 random decode workload using SGLang's CPU/GPU profiler.
- Analyze TP0's compressed Chrome trace by kernel/operator name and input
  tensor dimensions.
- Run GSM8K 50 after profiling as the correctness gate.

## Evidence

### Fused KDA + `f_b`

Kernel:

```text
kimi_k3_kda_decode_fb_bf16_gfx950
```

Observed launches on TP0: `138`.

This proves the incremental AITER boundary owns convolution, recurrent update,
gated RMSNorm, and the `f_b` projection during covered decode.

### MoE published output buffer

`aiter::fused_moe_` was called with a final BF16 output argument whose shape
matched the routed latent destination, e.g. `[509, 3584]`.

No `aten::copy_` event with `[M, 3584]` source/destination appeared in the
trace. Therefore the old routed-output D2D copy is absent.

### Routed-input contiguous copy

No `aten::contiguous` event with `[M, 3584]` appeared. Observed contiguous
events were KDA/prefill tensors such as `[M,1536]`, `[M,512]`, and
`[M,12,128]`.

Therefore K3's AITER MoE route consumes the fused-front strided view directly.

### Router correction-bias cast

No `aten::_to_copy` event with the expert-bias shape `[896]` appeared.
Observed casts were request metadata and attention shapes such as `[2]` and
`[M,12]`.

Therefore the per-layer router correction-bias cast is absent.

## Correctness gate

```text
GSM8K 50, 5-shot
score=1.000
latency=12.895 s
output throughput=413.896 tok/s
```

## Decision

Phase 1 passes all four evidence gates. AITER #4495/#4617 can proceed to
focused API/fake-op tests and local commit.

Note: `rocprofv3 --attach` successfully attached but did not emit output files
in this environment. The actionable evidence comes from the SGLang CPU/GPU
Chrome traces; all eight rank traces are retained.
